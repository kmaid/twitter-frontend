#import "./userFragments.graphql"

mutation AddRemoveCategoryToUser(
  $categoryId: Float!
  $userId: String!
  $add: Boolean!
) {
  addRemoveCategoryToUser(categoryId: $categoryId, userId: $userId, add: $add) {
    ...TwitterCardUserFragment
  }
}
